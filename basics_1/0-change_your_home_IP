#!/usr/bin/env bash
# coucou les coco
sudo sed -i '/\slocalhost\b/d' /etc/hosts
echo "127.0.0.2   localhost" | sudo tee -a /etc/hosts

sudo sed -i '/facebook.com/d' /etc/hosts
echo "8.8.8.8     facebook.com" | sudo tee -a /etc/hosts
